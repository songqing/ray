# pragma once 

//
// We use protobuf generated RPC stub (both client and server)
// as the common interface (but PB dependent).
//
// The implementation of the interfaces are embedded by
// the protobuf plugins, so we don't need to have a common
// interface here.
//
